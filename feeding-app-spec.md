# 宝宝喂养记录小程序 · 开发规格说明（方案2 · 小清新淡绿）

> 本文档配合原型文件 `baby_feeding_prototype_v2_mint.html` 一起使用。把两个文件都丢给 Claude Code，让它先读原型文件看实际交互效果，再按本文档的数据结构和技术方案实现微信小程序。

---

## 1. 项目概述

一个给爷爷奶奶等老年人使用的宝宝喂养记录微信小程序，替代手写记录本。核心诉求：

- **操作要简单**：首页大按钮 + 一步到两步完成一条记录
- **支持语音输入**：说一句话，调用大模型解析成结构化记录，用户确认后保存
- **数据要能查看/回溯**：今日记录支持按时间段筛选；历史记录按天查看

目标用户群体：老年人为主，其次是年轻父母。所有交互设计都要优先保证"老人能看懂、不容易点错、点错了能改"。

---

## 2. 技术栈建议

| 模块 | 建议方案 | 说明 |
|---|---|---|
| 前端框架 | 微信小程序原生 或 uni-app | 原生够用；如果未来要兼容 App/H5 再选 uni-app |
| 后端/数据 | 微信云开发（云数据库 + 云函数） | 省去自建服务器，个人/家庭项目足够；云函数里转发大模型请求 |
| 语音识别 | 微信小程序 `wx.getRecorderManager` 录音 + 微信同声传译插件 / 第三方 ASR API | 优先用微信官方插件，成本低 |
| 大模型解析 | DeepSeek API（或其他 LLM），通过云函数调用，不要把 API Key 放在前端 | 只输出结构化 JSON，prompt 见第 6 节 |
| 状态管理 | 小程序原生 `Page.data` 或 MobX-miniprogram | 数据量小，不需要复杂状态库 |

---

## 3. 设计规范（方案2 · 小清新淡绿）

### 3.1 色彩 Token

```css
--paper:        #F5FAF3   /* 页面/卡片主背景，浅薄荷白 */
--paper-line:   #D3E6CB   /* 分隔线、边框 */
--ink:          #33422E   /* 主文字色 */
--ink-soft:     #7E9078   /* 次要文字/说明文字 */

--primary:      #4F9668   /* 主色：森林绿，用于主按钮、强调卡片、时间线节点 */
--primary-soft: #DCEFDA   /* 主色浅底，用于选中态背景 */

--accent:       #E3A857   /* 强调色：暖橙，专用于"喂奶"相关数字和卡片，制造与绿色的对比 */
--accent-soft:  #FBEBD2

--success:      #3F8F7A   /* 用药/成功态，青绿色，与主色区分开 */
--success-soft: #DCEEE9

--warn:         #E38A72   /* 提醒/警示色，柔和珊瑚色，仅用于提醒类场景，不作为主色 */
--warn-soft:    #FBE2DA

--radius-card:  20px
--shadow-card:  0 6px 18px rgba(51,66,46,0.08)
```

设计原则：**不要把界面全部铺成绿色**。绿色只用在主操作（主按钮、主强调卡片）上；喂奶数据用暖橙色突出，制造视觉锚点，方便老人一眼找到"奶量"这种关键数字；用药类用青绿色区分于主色。

### 3.2 字体与排版

- 中文正文：系统默认 `PingFang SC` / `Microsoft YaHei`
- 页面主标题「宝宝喂养记录」：可选用楷体/宋体类衬线字体（`STKaiti` / `Noto Serif SC`），呼应"手写笔记本"的来源感，字号 24-26px
- 正文字号不低于 14px（老年用户可读性），关键数字（奶量 ml）字号 18-22px、加粗
- 行高 1.4-1.6，避免拥挤

### 3.3 间距与圆角

- 卡片圆角：20px（大圆角，视觉柔和）
- 卡片内边距：16-20px
- 按钮高度：不低于 44px（触控热区，老年用户手指定位不够精准）
- 功能卡片（喂奶/排便/睡眠/用药）之间间距：12px

---

## 4. 页面结构

### 4.1 首页（Home）

自上而下：

1. **顶部信息栏**：日期 + 星期 + 天气（沿用记录本习惯）
2. **上次喂奶卡片**（主色背景）：距上次喂奶多久 + 具体时间 + 一个进度条（表示距下次预计喂奶时间的进度，非精确计算，纯视觉提示）
3. **四宫格快捷入口**：喂奶 🍼 / 排便 💩 / 睡眠 😴 / 用药 💊
   - 点击"喂奶"→ 弹出底部选择框（喂养方式 + 常用奶量）
   - 点击"排便"→ 直接一键记录当前时间，无需二次确认弹窗（因为这个事件不需要额外信息）
   - 点击"睡眠"→ 弹出选择框（入睡 / 醒来）
   - 点击"用药"→ 弹出选择框（D3 / DHA / 益生菌 / 其他）
4. **今日记录时间线**：
   - 上方是时间段筛选按钮：全天 / 凌晨 / 上午 / 下午 / 晚上 / 自定义
   - 自定义筛选：点击后弹出开始时间 + 结束时间的选择框（`<input type="time">` 或小程序 `picker` 组件的 time 模式）
   - 时间线以竖线+圆点样式呈现，不同类型记录用不同颜色圆点（喂养=橙色、睡眠=绿色、用药=青绿、其他=灰绿）
   - 每条记录可点击「···」进行编辑/删除
5. **底部悬浮按钮**：「🎤 说一句话记录」，常驻不随滚动消失

### 4.2 语音记录弹窗

流程（原型中已实现的交互节奏）：

```
点击麦克风按钮
  → 弹窗打开，显示"正在聆听，请说话…" + 波形动画
  → 识别文字逐字显示（模拟语音转文字过程）
  → 转入"识别中…"状态
  → 展示解析结果卡片：大字号显示核心结果（如"奶粉 145ml"）+ 小字备注（如"加了 D3"）
  → 用户点击「确认保存」才真正写入记录；点击「重新说」可重新录音
```

**关键原则：语音识别结果永远需要用户手动确认，不自动保存。** 这是为了避免识别错误导致数据错误，同时让老人建立对工具的信任感。

### 4.3 历史记录页

- 顶部按天翻页（‹ 上一天 / 下一天 ›），显示日期 + 星期 + 天气
- 记录以表格形式呈现：时间 | 类型 | 数值 | 备注标签，视觉上用虚线分隔（呼应原本的手写横线本子）
- 底部显示当日小结：总奶量 + 喂养次数

---

## 5. 交互细节（供 Claude Code 参照原型 JS 逻辑）

以下逻辑已经在 HTML 原型里用原生 JS 实现，迁移到小程序时按同样的逻辑用 `setData` 重写即可：

- `openSheet(type)`：根据类型（feed/sleep/med）动态渲染底部选择框的选项（方式 + 数值）
- `pick(kind, value)`：记录用户在选择框里选中的选项，允许改选
- `confirmSheet()`：拼装选中结果为一条记录，写入今日记录列表，同时重置时间筛选为"全天"（保证新记录一定可见）
- `startVoice()` → `confirmVoice()`：语音识别与确认流程
- `setFilter(range)` / `applyCustomRange(start, end)`：按时间段筛选今日记录列表，无匹配时显示空状态提示文案
- `changeDay(delta)`：历史页翻页，重新渲染当天数据和小结

---

## 6. 数据模型

### 6.1 单条记录（Record）

```json
{
  "id": "string，唯一标识",
  "date": "2026-06-07",
  "time": "09:10",
  "type": "feed | poop | sleep | med | food",
  "feedMethod": "母乳 | 混合 | 奶粉",      // 仅 type=feed 时有值
  "amountMl": 145,                          // 仅 type=feed 时有值，单位 ml
  "sleepEvent": "入睡 | 醒来",              // 仅 type=sleep 时有值
  "medName": "D3 | DHA | 益生菌 | 其他",    // 仅 type=med 时有值
  "foodNote": "小米粥、南瓜泥（少许）",      // 仅 type=food 时有值
  "note": "备注，可为空",
  "source": "manual | voice",               // 记录来源：手动点击 或 语音识别
  "createdAt": "ISO时间戳"
}
```

### 6.2 当日小结（派生数据，不需要单独存储，前端/云函数按需计算）

```json
{
  "date": "2026-06-07",
  "totalMl": 760,
  "feedCount": 6
}
```

---

## 7. 语音输入 + 大模型解析设计

### 7.1 流程

```
用户录音 → 微信语音识别转文字（得到原始文本，如"喝了一百四十五毫升奶粉，加了D3"）
       → 云函数调用大模型 API，传入原始文本
       → 大模型仅返回结构化 JSON（不返回多余文字）
       → 前端渲染确认卡片
       → 用户确认后写入云数据库
```

### 7.2 大模型 Prompt 设计建议

系统提示词（放在云函数里，不要暴露给前端）：

```
你是一个母婴记录助手。将用户口述的一句话解析为结构化 JSON，只输出 JSON，不要输出任何其他文字或代码块标记。

字段要求：
- type: feed / poop / sleep / med / food 之一
- feedMethod: 母乳 / 混合 / 奶粉（仅 type=feed 时输出，否则不输出该字段）
- amountMl: 数字，单位毫升（仅 type=feed 时输出）
- sleepEvent: 入睡 / 醒来（仅 type=sleep 时输出）
- medName: 药品或补充剂名称（仅 type=med 时输出）
- foodNote: 辅食内容原文（仅 type=food 时输出）
- note: 其他备注信息，没有则不输出该字段

若用户的话里包含中文数字（如"一百四十五"），需转换为阿拉伯数字。
若信息不完整或无法判断类型，type 输出 "unknown"，note 里写下原始文本。
```

示例：

- 输入：`喝了一百四十五毫升奶粉，加了D3`
- 输出：
```json
{"type":"feed","feedMethod":"奶粉","amountMl":145,"note":"加了D3"}
```

### 7.3 需要注意的边界情况

- 识别结果为空或网络失败：前端要有明确的失败提示 + 「重新说」按钮，不能卡死在"识别中"状态
- 大模型返回非预期字段或格式错误：云函数需要做 JSON 解析容错，解析失败则返回 `type: unknown`，前端展示原始文本让用户手动选择分类

---

## 8. 建议开发顺序（MVP）

1. 首页静态布局 + 四个快捷入口的底部选择框交互（不含语音）
2. 云数据库记录的增删查（今日记录 + 历史记录）
3. 时间段筛选功能
4. 历史记录翻页视图 + 当日小结
5. 语音录音 → 转文字 → 大模型解析 → 确认保存 完整链路
6. 细节打磨：空状态、加载态、错误提示的文案和样式

---

## 9. 交给 Claude Code 时建议的说法

> "这是我们的产品设计文档和 HTML 静态原型，请先打开 `baby_feeding_prototype_v2_mint.html` 看一下实际的交互效果，再按 `feeding-app-spec.md` 里的数据模型和技术方案，帮我用微信小程序原生（或 uni-app）实现，先做 MVP 部分（首页四个入口 + 今日记录 + 时间筛选），语音识别部分先留接口，之后再接。"
